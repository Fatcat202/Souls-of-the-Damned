/// @description Insert description here
// You can write your code in this editor

// Failed attempt to clamp to application surface

x = clamp(xx, 0, room_width - global.cam_w);
y = clamp(yy, 0, room_height - global.cam_h);

xx = x;
yy = y;